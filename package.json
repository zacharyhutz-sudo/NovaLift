{
  "name": "novalift",
  "version": "0.9.4.2",
  "description": "Vertical mobile-first 2D rocket company prototype with improved world-view command switching, object actions, and calmer launch camera shake.",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "for file in src/*.js; do node --check \"$file\"; done"
  }
}
