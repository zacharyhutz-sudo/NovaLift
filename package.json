{
  "name": "novalift-v0.1",
  "version": "0.1.2",
  "description": "Portrait-first 2D space flight sandbox prototype for NovaLift.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "check": "node --check src/main.js && node --check src/game.js && node --check src/physics.js && node --check src/renderer.js && node --check src/input.js && node --check src/config.js"
  },
  "private": true
}
