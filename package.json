{
  "name": "novalift",
  "version": "0.8.0",
  "description": "Vertical mobile-first 2D rocket company prototype with campaign progression with satellite roles, orbital network scanning, and controllable command vessels.",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "node --check src/*.js"
  }
}
