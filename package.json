{
  "name": "novalift",
  "version": "0.9.9",
  "description": "Vertical 2D rocket company prototype with an in-game hangar hub launchpad UI pass.",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "for file in src/*.js; do node --check \"$file\"; done"
  }
}
